# Raomeo Management API

Rails API-only backend for the Raomeo Management hotel management system.

Use the root `README.md` for Docker Compose setup, seed credentials, API routes, and full-stack development commands.
